import React from 'react';
import Grid from "@material-ui/core/Grid";
import {ColumnTitle} from "./Timeline";

const Setting = () => {

    return (
        <Grid container justify="center">
            <ColumnTitle>Setting</ColumnTitle>
        </Grid>
    );
};

export default Setting;